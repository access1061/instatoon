# 실행 기록 — 인스타툰 생성 빠른 워크플로우 구현

## 작업 단위 결과

- S1 기존 라우팅과 스킬 절차 조사 — ✅ 안전 게이트는 유지하고 TDD·중복 확인 질문만 생략하도록 경계 확정
- S2 프로젝트 빠른 생성 규칙 구현 — ✅ `AGENTS.md`와 로컬 `fantasy-party-insta-toon` 스킬에 동일한 빠른 경로 반영
- S3 대표 요청 라우팅 검증 — ✅ 생성 요청·콘티 전용·참고 이미지 누락 세 경우와 패키징 조건 모두 통과

## 계획과 실제의 차이

- 유의미한 차이 없음.
- 프로젝트 진입 지침과 실제 작업 스킬 중 한 곳만 수정하면 규칙이 엇갈릴 수 있어 두 파일에 역할별로 같은 경계를 반영했다.
- 안전성을 위해 상충 지시, 필수 참고 이미지 누락, 자동 복구 불가능한 생성 실패는 질문 대상으로 유지했다.

## 완료 정의 검증

- TDD 자동 off: 통과
- 원고+이미지 생성 요청의 즉시 진행: 통과
- 콘티만 요청 시 생성하지 않음: 통과
- 필수 참고 이미지 누락 시 질문: 통과
- 첫 장 연속성 앵커 및 후속 장 병렬 생성: 통과
- 한글·캐릭터·의미 QA, 1080×1350, 100~500자 캡션: 통과
- `git diff --check`: 통과
