# Insta Toon Codex Workspace

이 작업 폴더는 한국어 원고를 손그림 본문 삽화 또는 인스타툰 스타일 이미지로 각색하기 위한 Codex 작업 환경이다.

## 기본 스킬

우선 사용할 스킬은 `skills/handdrawn-illustrations3`이다.

- 원고 분석, 인스타툰 각색, 말풍선 대화, 1~3컷 상황 구성에 사용한다.
- 스타일은 순백 배경, 검은색 얇은 사인펜 선화, 채색 없는 현대 미니 캐릭터다.
- `skills/handdrawn-illustrations3/assets/sample-line-style.png`와 `sample-line-style-2.png`는 선 스타일과 컷 구성만 참고한다.
- 샘플의 판타지 캐릭터, 엘프 귀, 망토, 장신구, 마법 요소는 사용하지 않는다.

보조 및 확장 스킬:

- `skills/colored-insta-toon`: 채색 인스타툰 및 컬러 삽화용 (파스텔톤 플랫 채색 및 캐릭터 일관성 유지 규칙 적용).
- `skills/flat-illustration`: 비툰(No-toon)형 기술 일러스트, 3D 클레이 스타일, 감성 수채화 선화용 (말풍선 및 글자 배제 규칙 적용).
- `skills/handdrawn-illustrations`: 기본 본문 삽화용.
- `skills/handdrawn-illustrations2`: 더 기발한 은유형 삽화용.

## 작업 흐름

1. 원고의 핵심 주장, 감정 흐름, 그림으로 설명할 인지적 앵커를 찾는다.
2. 사용자가 단순히 "원고/시나리오로 인스타툰 작성해줘"라고만 해도, 아래 자동 라우팅 기준으로 일반 생성 또는 Forge 루프를 선택한다.
3. 필요한 경우 먼저 shot list를 만든다.
4. 이미지 생성을 요청받으면 확인 질문 없이 한 장씩 생성한다.
5. gpt image-gen으로 생성할 때는 말풍선, 한글 텍스트 오버레이, 짧은 주석을 이미지 생성 프롬프트에 함께 포함한다. 기본적으로 후처리 조판으로 분리하지 않는다.
6. 한 이미지에는 하나의 핵심 의미만 담는다.
7. 생성 후 `skills/handdrawn-illustrations3/references/qa-checklist.md` 기준으로 점검한다.

## 자동 라우팅

사용자가 별도 지시 없이 시나리오나 원고를 붙이고 인스타툰 생성을 요청하면 다음 기준으로 진행한다.

- 1장짜리 단편으로 충분한 원고라면, 바로 생성하지 말고 "단편 1장으로 바로 만들지, Forge 루프로 shot list부터 잡을지"를 짧게 물어본다.
- 2장 이상이 자연스러운 원고, 정보량이 많은 원고, 금융·기술·정책처럼 오해 비용이 큰 주제, 반전/원인/결론이 나뉘는 시나리오, 말풍선·텍스트가 많아질 원고는 자동으로 Forge 루프를 적용한다.
- 사용자가 장수를 지정하면 그 장수를 우선하되, 과밀하면 Forge 루프 안에서 분리 제안을 하거나 지정 장수 내에서 정보를 줄인다.
- "빨리", "바로", "초안", "1장만"처럼 가벼운 요청이 명확하면 light 처리로 진행한다.

Forge 루프는 다음처럼 적용한다.

```text
Ask / Plan: 원고를 읽고 장수, 컷별 역할, 정확히 렌더링할 한글 문구를 정한다.
Run: 한 장씩 image_gen으로 생성한다.
Verify: 스타일, 한글, 과밀도, 파일 저장, 장별 역할을 점검한다.
Learn: 반복 규칙이나 수정 후보를 기록할지 판단한다.
Done: 저장 경로, 가장 안정적인 장, 수정 후보를 짧게 보고한다.
```

## 한글 텍스트 처리

- gpt image-gen은 한글 말풍선과 텍스트 오버레이를 비교적 안정적으로 생성하므로, 필요한 한글 문구는 생성 단계에서 이미지 안에 직접 넣는다.
- 말풍선과 텍스트 오버레이는 원고의 핵심 내용을 충분히 전달하도록 사용하되, 한 장에 너무 많은 문장을 넣지 않는다.
- 각 말풍선은 짧은 1문장 중심으로 쓰고, 텍스트 오버레이는 1~2줄 중심으로 쓴다.
- 정확히 들어가야 하는 문구는 프롬프트에 `Text to render exactly:` 형식으로 따로 명시한다.
- 후처리 조판은 사용자가 명시적으로 원하거나, 생성 결과의 한글이 실제로 깨졌을 때 수정용으로만 사용한다.

## 저장 규칙

이미지 결과물은 날짜별 폴더에 저장한다.

```text
assets/YYYY-MM-DD/
```

파일명은 순서와 주제를 포함한다.

```text
01-topic-name.png
02-topic-name.png
```

같은 파일명이 있으면 덮어쓰지 않고 `-v2`, `-v3`을 붙인다.

## 금지

- 기존 결과물 덮어쓰기.
- 여러 장을 한 이미지에 억지로 합치기.
- PPT 흐름도, 상업 일러스트, 컬러 웹툰 키비주얼처럼 만들기.
- 판타지 종족, 엘프 귀, 마법 소품, 망토, 갑옷, 보석 장신구 사용.
