# Insta Toon Codex Workspace

이 작업 폴더는 한국어 원고와 사용자 참고 이미지를 인스타툰·본문 삽화로 각색하는 환경이다.

## 기본 스킬

우선 `skills/handdrawn-illustrations3`을 사용한다. 이름과 관계없이 흑백 손그림에 고정하지 않으며, 사용자 지시와 참고 이미지에 맞춰 채색·선화·캐릭터·종족·구도·화면 비율을 선택한다.

보조 스킬은 사용자가 특정 스타일을 명시했을 때 선택한다.

- `skills/colored-insta-toon`: 파스텔 플랫 컬러.
- `skills/flat-illustration`: 비툰형 기술·3D·수채화 삽화.
- `skills/handdrawn-illustrations2`: 은유형 손그림 삽화.
- `skills/pixel-modern-insta-toon`: 픽셀아트 인스타툰.

## 의도 우선순위

1. 사용자가 명시한 장수, 비율, 스타일, 캐릭터, 문구.
2. 첨부하거나 지정한 참고 이미지.
3. 원고의 핵심 주장과 감정 흐름.
4. 정보가 없을 때만 스킬 기본값.

참고 이미지는 실제 이미지 입력으로 전달한다. 선·색·비율·표정·구도·캐릭터 중 사용자가 원하는 범위를 분석해 반영하고, 기존 프리셋을 이유로 반대 특성을 강제하지 않는다.

## 간결한 작업 흐름

```text
Read → Plan → Generate → Check → Package
```

1. **Read:** 원고의 핵심 메시지와 참고 이미지의 시각 특징 3~6개를 찾는다.
2. **Plan:** `장 역할 / 핵심 의미 / 캐릭터 행동 / 정확한 문구`만 정한다.
3. **Generate:** `image_gen`으로 한 장씩 생성하고 참고 이미지를 함께 전달한다.
4. **Check:** 사용자 의도, 캐릭터 일관성, 의미, 한글, 과밀도를 확인한다. 가까운 결과는 부분 편집한다.
5. **Package:** 날짜별 폴더에 이미지와 100~500자의 게시용 인스타 캡션을 함께 저장·전달한다.

인스타 피드·캐러셀은 생성 결과의 실제 크기를 확인하고 `skills/handdrawn-illustrations3/scripts/fit_instagram_4x5.py`로 정확히 `1080×1350`에 맞춘다. 원본을 자르거나 늘이지 않고 비율 유지 축소 후 흰 여백만 추가한다. 크기 검사를 통과한 파일만 최종본으로 전달한다.

## 장수 라우팅

- 사용자가 지정한 장수를 우선한다.
- 핵심 메시지 하나면 확인 질문 없이 1장으로 진행한다.
- 문제·원인·전환·결론 중 둘 이상이 독립적이거나 한 장이 과밀하면 2~5장으로 나눈다.
- shot list는 사용자가 기획을 요청했을 때만 먼저 보여준다. 생성 요청이면 내부 계획 후 바로 진행한다.

## 캐릭터와 스타일

- 사용자가 제공한 캐릭터는 사람·동물·판타지·로봇·마스코트 여부와 관계없이 의도대로 반영한다.
- 시리즈는 첫 안정 결과를 스타일 앵커로 삼아 이후 장에 다시 참조한다.
- 참고 이미지에서 스타일만 가져올지 캐릭터도 유지할지 사용자의 표현을 따른다.
- 지시가 없을 때만 16:9, 순백 배경, 검은 사인펜 선화, 미니 캐릭터를 기본값으로 사용한다.

## 한글 텍스트

- 정확한 문구는 프롬프트의 `Text to render exactly:` 아래에 명시한다.
- 말풍선과 오버레이는 짧게 유지한다.
- 한글이 틀리면 문구를 줄여 한 번 재생성하고, 계속 틀리면 부분 편집한다.
- 사용자가 요청하지 않은 문구는 추가하지 않는다.

## 저장 규칙

```text
assets/YYYY-MM-DD/01-topic-name.png
```

같은 이름이 있으면 `-v2`, `-v3`을 붙여 기존 결과를 보존한다. 여러 장은 각각 별도 이미지로 저장한다.

## 게시용 캡션

- 이미지 생성 후 별도 요청이 없어도 공백 포함 100~500자 캡션을 함께 작성한다.
- 500자를 채우지 말고 원고의 정보량에 맞춰 핵심이 충분히 전달되는 가장 짧은 길이를 선택한다.
- 그림 대사를 반복하지 않고 이미지에서 생략된 배경, 작동 원리, 사용법, 수치, 한계와 주의점을 보완한다.
- 필요한 경우 호기심을 잇는 도입, 보충 설명, 활용 또는 주의, 짧은 행동 유도, 관련 해시태그 3~8개로 구성한다.
- 캡션은 이미지와 함께 전달하고 `assets/YYYY-MM-DD/instagram-caption-{topic}.md`로 저장한다.
